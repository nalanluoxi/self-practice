# 冀子彦
icon:user 男 / 2005.04.03 ｜ icon:phone 184-3543-2214 ｜ icon:email [adrian0304@qq.com](mailto:adrian0304@qq.com) | icon:wechat adrian050304
## icon:user 教育背景 Education Background
::: start
icon:taiyuanligong - **太原理工大学 - 软件学院 - 软件工程 - 211**

:::
**2023-09-01 至 2027-07-01**

::: end
## icon:practice 实习经历 Internship Experience
**公司名称**：icon:meituan 北京三快科技有限公司（美团）
- **实习时间**：2025.8 - 2026.5

- **实习内容**：

作为后端工程师参与 Friday 数据组迭代开发，负责为 LongCat 大模型训练构建预训练语料库，主导建立**网页抓取 → 解析 → 清洗**全流程体系，重点参与 **Sitemap 新链发现环路**的架构设计与需求落地。

**个人产出：**

- **多线程并发重构与消息队列扩容**：将单线程同步消费模式重构为**异步线程池架构**，消息拉取与业务处理彻底解耦；同步完成 Mafka 集群从 200 分区至 400 分区的翻倍扩容，**小时抓取量从 2.5 万跃升至 96 万，峰值 QPS 达 400，整体吞吐提升 16 倍**。

- **Sitemap 发现环路三级限流体系**：针对大站持续挤兑小站问题，设计**集群层 / 单机层 / 站点层**三级限流防护网，前置黑名单、URL 合法性校验、Redis 去重拦截；进一步实现「**线程池健康度 - 限流阈值**」动态联动模型，系统稳定性大幅提升。

- **GZIP 压缩 Sitemap 解析能力补齐**：通过日志分析发现约 4% 的 Sitemap 流量（日均 400 万+）为 `.gz` 格式被直接丢弃，基于 `GZIPInputStream` 实现流式解压并无缝对接原有处理链路，**新链发现量峰值从 3.79 亿激增至 52.6 亿**。

- **站点频度调度策略建设**：针对全量站点每日固定调度的低效问题，引入**三维打分模型**，将下游调度结果反馈至打分系统形成闭环，实现高频站点加速、低频站点降载的精细化资源调度，**日链接产出贡献量达 900 亿+**。

- **DSL 规则引擎治理垃圾/作弊站点**：对 MDP 配置中的正则表达式预编译为 Pattern 缓存，通过配置监听实现热更新、秒级生效；拦截逻辑前置于 Redis 去重和限流之前，避免作弊站点消耗有效资源，**日均拦截量达 4~5 亿**。

- **网页截图抓取全链路服务建设**：面向多模态大模型「看图写代码」训练需求，独立设计并实现截图抓取全链路服务；基于 Playwright 引擎全页面渲染截图，构建**独立线程池**与 Sitemap 环路完全隔离，截图结果上传 S3；集群 490 台机器整体吞吐约 **1000 条/分钟**，产出数据用于大模型多模态能力训练。

## icon:project 项目经历 Project Experience
- **项目名称**：**智析RAG**

- **项目介绍**：基于 **Spring AI** 构建企业级检索增强生成（RAG）对话系统，支持 **PDF / Markdown / TXT** 多格式文档解析入库与语义问答，涵盖多通道检索、意图分类、熔断路由、对话记忆等核心模块。

- **多通道并行检索**：基于 `CompletableFuture` 同时发起全局向量检索与意图定向检索两条通路，意图定向结果优先合并去重，有效提升召回率与检索精度。

- **Chat / Embedding 双维熔断路由**：为 Chat 与 Embedding 分别实现多模型候选三态熔断器（CLOSED → OPEN → HALF_OPEN），连续失败自动熔断、定时探测恢复；Chat 路由额外引入首包探测，拦截空流响应触发切换，实现 AI 服务零感知故障转移。

- **意图树三级分类缩小检索空间**：构建领域 → 类目 → 话题三级意图树，通过 LLM 对用户查询分类后将检索范围收敛至目标知识库；对闲聊/系统意图短路处理，跳过向量检索直接回复，降低无效调用开销。

- **全流水线降级容错**：RAG 流水线（改写 → 分类 → 检索 → 重排 → Prompt 组装）各阶段独立捕获异常并降级，改写失败回退原始 Prompt，检索失败触发兜底回复，任意单点故障不阻断整条链路。

- **LLM 重排序提升上下文质量**：向量检索召回后，用 LLM 对每个文档片段与问题的语义相关性打分（0~10），按分数降序取 Top-3 注入 Prompt，相比纯余弦相似度排序显著提升答案准确率。

- **持久化对话记忆与摘要压缩**：基于 PostgreSQL 持久化多轮对话记忆，当会话消息超过阈值（默认 10 轮）时自动调用 LLM 压缩最旧的一半消息为摘要写回数据库，在保留长期上下文的同时控制 Token 消耗。

## icon:technology 专业技能 Professional Skills
1. 熟悉 Java 语言，熟悉面向对象设计思想，熟悉 Java 常用集合，**熟悉 HashMap 的底层原理**。
2. 熟悉 JUC，熟悉 synchronized 关键字，了解 ThreadLocal，熟悉线程池的使用与调优。
3. 了解 JVM 内存模型，了解**双亲委派机制**，了解 JVM 垃圾回收算法。
4. 熟悉 **Spring**、**Spring MVC**、**Spring Boot**、**MyBatis** 等框架，掌握 IoC、AOP 的使用。
5. 熟悉 **MySQL** 的使用，了解 MySQL 的事务、存储引擎、索引、MVCC 机制，具有一定的 SQL 优化能力。
6. 熟悉 **Redis** 的常见数据结构和使用场景，熟悉**缓存穿透、击穿、雪崩**的常见解决方案。
7. 熟悉 **Kafka/Mafka** 的基本使用，具备分区扩容、异步消费、消息可靠性投递及重复消费幂等处理的实战经验。
8. 了解 Git 的使用，可进行团队协同开发和代码管理。